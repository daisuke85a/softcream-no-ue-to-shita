# ソフトクリーム積み 仕様書

ブラウザで遊べる、ソフトクリームをキャッチして積み上げるゲーム。
スマホでの操作を主に想定。

公開URL: https://daisuke85a.github.io/softcream-no-ue-to-shita/

## ゲームの目的

落ちてくるソフトクリームを、人間が持つコーンで受け止めて積み上げる。
頭にソフトが落ちるか、地面の山で動けなくなるか、スタックが画面上端に届くまで、
できるだけ多くキャッチしてスコアを稼ぐ。

## 操作

- スマホ: 画面上のタッチ位置が人間の**目標 x** になり、人間はそこまで歩いて**ぴたっと停止**（オーバーシュートしない）。指をスライドすると目標が追従。動いている最中にタッチを離せば即停止。
- PC マウス: 同様にクリック保持中のカーソル位置が目標 x になる。
- PC キーボード: `←` / `→`（または `A` / `D`）を押している間動く。
- 人間は左右にのみ移動。上下移動なし。移動速度は 280px/秒。
- 右端の上限は **コーン開口部の右端が画面右端に到達する位置**（= 体中心 286px）。それ以上右には移動できない。

## キャラと持ち方

- 人間はソフトクリームのコーンを**右斜め上**に向けて持つ。
  - コーンの開口部（=スタックが乗る位置）は人間の体の中心から
    **右に 52px** オフセットしている。
  - 「タップした位置に人間が動く」→ そのまま動くと「コーンはその右上に出る」。
  - 落ちてくる位置に体を合わせるのではなく、**コーンを合わせる**意識が必要。

## ソフトクリーム

- 色は3種類: **黒・白・赤**
- サイズは横 72px、縦 32px（ピクセルアート風）
- 上の先端（ツノ）の描画は、落下中のスクープと、積み上げた一番上の段にのみ表示。

## キャッチ判定

- 落ちてきたソフトの中心 x が、**コーン位置 ±36px**（=スタックの横幅と同じ）の範囲を初めて横切った瞬間にキャッチ成功。
  - 一度通り過ぎたら、後から横移動して合わせてもキャッチ不可。
- キャッチ成功時の挙動:
  - 直前にスタックの一番上が**同じ色**: 1つのスクープのまま、高さが **+11px** 成長
  - **違う色**: 新しいスクープとしてスタックに追加（高さ +32px）

## ゲームオーバー条件

1. **頭にソフトが落ちる（メイン）**
   - キャッチに失敗したソフトが、人間の頭の上 (`y = 床 - 60`) に到達し、
     その瞬間に人間の体の中心 ±25px に x がある → 即ゲームオーバー
2. スタックの一番上が画面の上端 (`y ≤ 8`) に達する
3. 地面の山で 5 カラム全部が塞がる

## お客さん配達

- 画面右端 (x = 340) に**お客さん**が立っている。
- 人間が右端 (`MAX_BODY_X = 286`) に到達した瞬間、スタックがあれば**自動でお客さんに配達**。
  - スコアに `+10 pt` 加算（基本配達スコア）。
  - 配達時のスタック上端が **BONUS LINE (y = 40)** 以上なら、追加で **`+50 pt` ボーナス**。
  - 配達のたびに落下スピードが `+20px/秒` 加速（上限 350px/秒）。
- 配達するとスタックは空になり、最初から積み直し可能。
- BONUS LINE と画面上端ゲームオーバーラインの間は `PIECE_H = 32px = ソフト 1 個分`。攻めればもう 1 個積めるが、超えるとゲームオーバー。
- 旧「スタックを画面上まで積んだら GAME CLEAR + 100」は撤廃。クリア条件は無く、繰り返し配達でスコアを伸ばすゲームに。

## ヒヤリ判定（焦り顔）

- 足元から 250px 以内まで落ちてきたソフトが、人間の体の中心 ±29px に x が
  揃っている場合、人間の顔が**焦り顔**に切り替わる:
  - 白目つきの大きく見開いた目
  - 困り眉（内向きに傾く）
  - ぽかんと開いた口
  - 右こめかみに青い汗マーク
- 横に動いて hit zone を外せば、通常顔に戻る。

## 持っているソフトクリームへの側面衝突

- 落下中のソフトクリームが、持っているスタックの**縦方向の範囲**（コーン開口部より上）に
  入っており、かつスタックの中心から `PIECE_W`（=72px）以内に近づいているとき、
  その方向への移動は凍結される（反対方向への退避は可）。
- 例: 右側から落ちてくるソフトがスタックの右側面に当たりかけたら、人間は右に進めなくなる。左には逃げられる。
- 落下中ソフトがスタック y 範囲を抜ければ凍結解除。

## 地面に落ちたソフト

- キャッチできずに地面 (`y = 590`) に達したソフトは、その x 位置のカラムに積もる。
- 山があるカラムは人間が通れない（動こうとしても山の手前で止まる）。
- 山が立っているカラムに人間が居る場合、隣の空きカラムに押し出される。
- 落ちたソフトは**3ターン**（= 新しいソフトが3つスポーンする間）で溶けて消える。
  - 上に積み重なった山の中の古いピースから順に消える。

## 難易度カーブ

- 4ターン（ソフトを4つスポーン）ごとに:
  - スポーン間隔が短くなる（最短 650ms）
  - 落下スピードが上がる（最高 260px/秒）

## 画面構成

- 論理解像度: 360 × 640（9:16 縦長）
- 上部 36px は HUD（スコア / ターン数 / 危険ライン点滅）
- 床位置: y = 590
- カラム数: 5（= 画面幅 ÷ ソフト幅 72px）

## 技術メモ

- 単一 HTML ファイル (`index.html`) で完結。外部依存なし。
- Canvas 2D で描画、`image-rendering: pixelated` でドットを保つ。
- DPR (デバイスピクセル比) は最大 2倍までスケールアップ。
- タッチ操作は `touch-action: none` でスクロールを抑制。
- requestAnimationFrame ループ、dt 上限 0.05 秒。

## 主要な定数（`index.html` 内）

```javascript
PIECE_W = 72           // ソフトクリームの横幅
PIECE_H = 32           // ソフトクリームの高さ
MERGE_GROWTH = 11      // 同色マージで追加される高さ
PLAYER_W = 60          // 人間の横幅
CONE_TOP_OFFSET = 71   // 足元からコーン開口部までの高さ
CONE_X_OFFSET = 52     // 体の中心からコーン中心までの x オフセット
CATCH_TOL = 36         // キャッチ判定の x 許容幅（半幅）
HIT_TOL = 25           // ゲームオーバー判定の x 許容幅
HEAD_TOP_DY = 60       // 足元から頭頂部までの高さ
```
